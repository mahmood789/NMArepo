% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{audit_nma_datasets}
\alias{audit_nma_datasets}
\title{Audit dataset accessibility and schema conformance}
\usage{
audit_nma_datasets(include_remote = TRUE, install_if_missing = FALSE)
}
\arguments{
\item{include_remote}{Logical; include datasets that rely on external CRAN packages. Defaults to \code{TRUE}.}

\item{install_if_missing}{Logical; passed to \code{\link[=load_nma_dataset]{load_nma_dataset()}}. When \code{TRUE} the function will attempt to install any required packages for remote datasets.}
}
\value{
A tibble with one row per dataset entry including status information and any associated error messages.
}
\description{
Iterates over the dataset registry and attempts to load each entry using \code{\link[=load_nma_dataset]{load_nma_dataset()}}. The function captures any errors and returns a tibble summarizing which datasets are immediately accessible under the current R session configuration.
}
\examples{
audit <- audit_nma_datasets(include_remote = FALSE)
audit$status
}
