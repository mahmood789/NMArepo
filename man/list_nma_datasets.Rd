% Generated by hand to satisfy R CMD check requirements
\name{list_nma_datasets}
\alias{list_nma_datasets}
\title{List available network meta-analysis datasets}
\description{
Return a tibble enumerating datasets shipped with the
\pkg{netmetaDatasets} package along with clinical and licensing metadata.
Remote-only entries are included by default so researchers can discover
external resources bundled with other repositories.
}
\arguments{
  \item{include_remote}{Logical indicating whether to include datasets
  that require loading from external packages. Defaults to TRUE.}
}
\value{
A tibble with one row per dataset entry.
}
\examples{
list_nma_datasets()
}
